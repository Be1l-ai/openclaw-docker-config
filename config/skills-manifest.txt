# OpenClaw Skills Manifest — Hugging Face Spaces
# Auto-installed on container startup via entrypoint-hf.sh.
# Add a ClawHub skill name (one per line) and rebuild to install.
# Browse available skills: https://clawhub.ai/

# YouTube — transcript fetching, video search, channel lookup. No API key needed.
yt

# Agent Browser — headless Chromium for JS-heavy / paywalled pages
agent-browser

# Conventional Commits — format commit messages properly
conventional-commits

# GitHub — interact with GitHub (issues, PRs, CI runs) via gh CLI
github
